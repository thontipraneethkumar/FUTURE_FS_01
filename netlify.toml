[build]
  publish = "."
  functions = "netlify/functions"

# Lets the frontend always call /api/contact regardless of host —
# on Netlify this redirect forwards it to the actual function.
[[redirects]]
  from = "/api/contact"
  to = "/.netlify/functions/contact"
  status = 200
